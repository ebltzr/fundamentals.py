import shutil 

shutil.copyfile('reading-files.txt', 'copy.txt') # source (src), destination (dst)
    # this created a new txt file called copy.txt that is a copy of reading-files and it's contents in the python resources folder 

# for copy() and copy2() also uses the same format